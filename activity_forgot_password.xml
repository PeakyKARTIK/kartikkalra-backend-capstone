<?xml version="1.0" encoding="utf-8"?>
<LinearLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="24dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Email Input -->
    <EditText
        android:id="@+id/emailInput"
        android:hint="Enter your email"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <!-- First Security Question Input -->
    <EditText
        android:id="@+id/answer1"
        android:hint="What's your first pet name?"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <!-- Second Security Question Input -->
    <EditText
        android:id="@+id/answer2"
        android:hint="What's your first school name?"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <!-- Third Security Question Input -->
    <EditText
        android:id="@+id/answer3"
        android:hint="What's your cloth size?"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <!-- Submit Button -->
    <Button
        android:id="@+id/submitBtn"
        android:text="Verify"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"/>

    <!-- Result Text -->
    <TextView
        android:id="@+id/resultText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text=""
        android:textColor="#FF0000"/>

</LinearLayout>
